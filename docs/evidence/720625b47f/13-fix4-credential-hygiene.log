=== Fix 4, code-side half, on frozen SHA 720625b47f ===
The 3 Sep 2026 walkthrough demoed the live admin panel. Fix 4 asks that the admin
password AND account email used in that demo no longer work, with the Owner's own
attestation (docs/CLIENT_MEETING_AUDIT_AND_IMPROVEMENTS.md Section 1B action table,
Section 3 item 4). PR7 can prove the repository half and hand the live half a
verifiable checklist; it cannot and must not touch the live database or assert on
someone else's mailbox.

--- 1. No shipped file seeds, embeds or defaults a live credential ---
user INSERTs in migrations:
  count: 0
password literals in migrations/seeds (informational prose excluded):
  (empty above = none)
demo admin emails in any seed or config committed:
  (empty above = none; .env.example carries no password, only placeholders)

--- 2. The rotation machinery the Owner will use exists and is audited ---
  Every rotation endpoint stamps users.password_changed_at (migration 011), which Rbac
  compares per request as pwd_epoch, so rotation signs every old session out:
    api/v1/users.php:168   admin set_password (the Owner's route for staff resets)
    api/v1/auth.php:306    self-service password change
    api/v1/auth.php:385    token reset completion
    api/v1/auth.php:465    admin-initiated forced change
  The one un-stamped write, api/v1/auth.php:161, is the login-time rehash upgrade;
  stamping there would kick out the signing-in user, and it is correctly absent.
  public/setup.php is fail-closed (no SETUP_TOKEN = 404). After the first Owner the
  token line must be REMOVED from the server .env (DEPLOYMENT note 8.5 / plan 8.5):
    10: *   - Fails closed: if SETUP_TOKEN is not set in .env, this returns 404.
    11: *   - A wrong or missing token returns 404, so it does not reveal it exists.
    16: * Set a strong SETUP_TOKEN in the server .env (openssl rand -hex 32), create the
    17: * Owner, then remove SETUP_TOKEN from .env. No exception is ever shown here; the

--- 3. Live verification checklist handed to the Owner (append output to this pack) ---
  Run in cPanel phpMyAdmin against ibbbnlso_okveggies (or read via the admin Users screen):

  V1  Who is the Owner now, and when did the password last change?
      SELECT id, email, user_type, status, password_changed_at
        FROM users WHERE user_type = 'owner' ORDER BY password_changed_at DESC;
      PASS: the only row carries the Owner's real email, and
      password_changed_at >= the handover rotation time recorded by the Owner.
      FAIL: any row still showing an email used during the 3 Sep walkthrough,
      or password_changed_at IS NULL / older than the meeting date.

  V2  The demo-era password cannot sign in (Owner tries the OLD password once,
      in the live admin login, then immediately closes the session):
      PASS: 'Those details do not match an active account.'
      (If it signs in, treat as a breach: rotate again from a manager session
      and record the incident.)

  V3  No second backdoor account: SELECT COUNT(*) FROM users WHERE status='active';
      compare against the named staff list the client expects; each active staff
      account must have a named human behind it (admin/users.php screen).

  V4  SETUP_TOKEN removed: curl -i https://okveggies.com.ng/public/setup.php
      returns 404, and the server .env has no SETUP_TOKEN line (plan Section 8.5).

  V5  Attestation line, signed and dated by Kumbish Emmanuel Putleh (or his delegate):
      'I confirm the admin password used during the 3 September 2026 demo no longer
      works on okveggies.com.ng, that the account email was changed to ______ on
      ______ (date, UTC), and that no demo credentials remain usable. I have not
      shared the new password outside the named staff list.'
